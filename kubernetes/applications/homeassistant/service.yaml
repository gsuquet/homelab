apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: homeassistant
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: homeassistant
    app.kubernetes.io/part-of: homeassistant
    app.kubernetes.io/version: "2026.1.3" # renovate: datasource=github-tags packageName=home-assistant/core
  name: homeassistant
  namespace: homeassistant
spec:
  ports:
    - name: http
      port: 8123
      protocol: TCP
      targetPort: 8123
  selector:
    app.kubernetes.io/instance: homeassistant
    app.kubernetes.io/name: homeassistant
  type: ClusterIP
